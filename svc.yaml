apiVersion: v1
kind: Service
metadata:
  name: eldia-service
  namespace: eldia
spec:
  selector:
    app: snk
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
  -----
apiVersion: v1
kind: Service
metadata:
  name: marley-service
  namespace: marley
spec:
  selector:
    app: marley
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
  type: NodePort
  
